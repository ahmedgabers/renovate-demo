apiVersion: v2
name: wrapper-cert-manager
description: "Wrapper chart for cert-manager (test)"
version: "v1.0.0"        # try with/without the leading v if you want
dependencies:
  - name: cert-manager
    version: "v1.17.0"   # deliberately old so Renovate finds an update
    repository: https://charts.jetstack.io
